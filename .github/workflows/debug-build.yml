name: Debug build APK

on:
  pull_request_target:
    branches:
      - "main"
    paths-ignore:
      - ".gitignore"
      - "**.md"
      - "LICENSE"
      - ".idea/**"
      - ".github/**"
      - ".editorconfig"
      - "images/**"
      - "metadata/**"

jobs:
  run:
    name: Build debug APK
    runs-on: macos-14

    permissions:
      pull-requests: write

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          distribution: "oracle"
          java-version: "17"
          cache: "gradle"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Set up Python for native libraries
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Prepare NPU libraries
        run: |
          sdkmanager "ndk;28.1.13356709"
          python3 scripts/prepare_npu_runtime.py

      - name: Build debug APK
        run: |
          ./gradlew assembleDebug

      - name: Upload debug APK
        uses: actions/upload-artifact@v7
        id: artifact-upload-step
        with:
          name: debug-${{ github.sha }}
          path: ${{ github.workspace }}/app/build/outputs/apk/debug/app-debug.apk

      - name: Comment APK Link
        uses: thollander/actions-comment-pull-request@v3
        with:
          comment_tag: apk
          message: |
            Build complete! Here's the debug APK: ${{ steps.artifact-upload-step.outputs.artifact-url }}
